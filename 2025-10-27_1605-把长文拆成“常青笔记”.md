# 标题
做一个帮我把长文拆成“常青笔记”的知识加速器

## 关键洞察
- 常青笔记是“可独立成立的最小知识单元”，应以可复用的陈述句为题，聚焦一个概念/主张/机制，并附范围条件与证据来源。
- 要把长文拆成常青笔记，核心是三步：识别要点（概念/主张/机制/流程/反例/开放问题）→ 原子化重写 → 网络化连接（互链、上下位、因果、对立）。
- 标题即论点：好标题应是永不过时的可移植结论，而非文章情境或时间相关表述。
- 证据与溯源是质量保障：每条笔记都要附引用片段与定位信息（页码/段落/URL+时间戳），便于复核与更新。
- 连接优先于分类：通过自动建议“近义/上下位/因果/对立/同证据来源”链接，让笔记在图谱中获得语义位置。
- 人机协同是必须：模型负责候选笔记生成与链接建议，人类进行合并/拆分/定标题/加范围条件，持续校正形成“偏好记忆”。
- 评估要看“覆盖率×原子性×连接度×可复用性×编辑成本”，而不是摘要相似度。
- 输出要与日常知识库无缝对接：Markdown+YAML 前言、双向链接语法、跨工具同步（Obsidian/Logseq/Notion）。
- 渐进式提炼更稳妥：先抽取关键片段，再生成草案笔记，最后升格为常青笔记，避免一次性过拟合原文表述。
- 通过小型本地嵌入与规则增强，能显著降低大模型成本，同时提升一致性与可控性。

## 可能的行动步骤
1) 定义常青笔记模型（模板）
- 必填字段：id（稳定短ID）、title（陈述句）、type（概念/主张/机制/流程/模式/反例/问题）、summary（100–200字）、scope（适用条件/边界）、evidence（引用片段+定位）、source（书/文献/URL+时间）、links（related/up/down/causal/opposite）、tags、last_reviewed、confidence。
- 风格规范：标题为可独立成立的陈述句；正文避免上下文依赖语；明确何时成立、何时不成立；给出最小反例。

2) 建立拆分与提炼流程（流水线）
- 文档清洗：提取正文、去脚注广告、保留结构（标题层级、图表说明）。
- 语义分段：混合规则与模型（TextTiling/段落+话题转折检测）得到意义段。
- 角色标注：把段落标成“定义/主张/证据/机制/流程/反例/问题/结论”等（论证单元识别）。
- 术语与概念抽取：关键短语与命名实体（TextRank/YAKE+嵌入相似度）。
- 聚类与去重：将相近片段合并为候选笔记主题簇。
- 草案生成：为每簇生成候选标题、要点、范围条件与证据清单。
- 原子性检查：自动检测是否“一条笔记多主张”，给出拆分建议。

3) 连接构建（知识图）
- 近义/同义：嵌入相似度+重述检测。
- 上下位：定义句式与包含关系规则+监督样例。
- 因果/机制：触发词与事件链抽取。
- 对立/反例：否定/反驳语气与矛盾检测。
- 证据共现：共用来源或相邻页码的链接建议。
- 输出双向链接建议与信心分，并允许一键接受/拒绝。

4) 人机协同编辑界面（MVP）
- 左侧：长文与高亮片段；中间：候选笔记队列；右侧：当前笔记模板表单。
- 快捷操作：拆分/合并/设为反例/设为机制/改标题为陈述句/添加范围条件。
- 质量仪表：原子性评分、证据完整度、链接密度、重复度。

5) 输出与同步
- 以 Markdown+YAML 前言落盘；文件名使用 id+slug；正文引用原文片段并附来源链接与定位。
- 支持 Obsidian/Logseq 双链语法，自动更新反向链接索引。
- 支持 Notion/数据库字段映射与导出。

6) 提示词与规则库（可复用组件）
- 拆分提示：从段落中抽取“可独立成立的陈述+范围+证据引用”。
- 标题提示：把要点改写为“普适的、可移植的陈述句”，避免时间/情境词。
- 原子性检查提示：检测是否包含多个彼此独立的主张并给出拆分方案。
- 链接建议提示：基于现有库，返回5条上位/下位/因果/对立候选及理由。
- 风格守则清单：禁止模糊词；必须附范围与反例；证据引用必带定位。

7) 模型与基础设施
- 生成与改写：通用大模型（可选云端）+ 本地小模型做草案与校对。
- 嵌入与检索：多语种嵌入模型（如 bge-m3）+ 本地向量库（FAISS/SQLite）。
- 断点与版本：Git 管理笔记变更，保留人审轨迹供回放与对比。
- 性能：批处理长文；对比抽取→聚类→生成的流水线并行化。

8) 质量与评估
- 覆盖率：长文关键主张被覆盖的比例（人工基准集）。
- 原子性：平均每条笔记是否仅包含1个主张（审阅打分）。
- 连接度：平均出入度、连通分量大小、孤点率。
- 可复用性：在新主题查询时被复用/引用的次数。
- 编辑成本：每千字编辑用时、接受率、修改率。

9) 数据与合规
- 私有化优先：默认本地推理与本地嵌入，云端需显式授权。
- 源文版权与引用策略：保留短引用并标注出处；长引遵从许可。
- 敏感信息检测：PII/机密条款自动遮罩与脱敏。

10) 路线图（从0到1）
- 第1周：确定模板与规则库；做手工金标10篇长文。
- 第2–3周：完成抽取-聚类-草案流水线与基本UI。
- 第4周：加入链接建议与原子性检查；接入 Obsidian 同步。
- 第5–6周：人审反馈闭环与主动学习；打通评估面板。
- 第7周：小范围内测，优化提示与规则，降本增速。

11) 小示例（示意）
- 标题：有效的笔记标题应是可独立成立的陈述句
  摘要：标题写成普适陈述能提升可复用性与跨情境检索效果...
  范围：适用于知识库而非新闻类日志
  证据：引自 Matuschak《Evergreen notes》段落X
  相关：上位-常青笔记原则；对立-情境化标题在回顾中更具情感价值

## 延伸阅读方向
- Evergreen notes（Andy Matuschak）：Evergreen notes; Evergreen notes should be atomic; Evergreen note titles are like APIs
- Zettelkasten 与 Luhmann 卡片盒：思想如何连接与编号系统的价值
- How to Take Smart Notes（Sönke Ahrens）：从文献笔记到永久笔记的流程
- Progressive Summarization（Tiago Forte）：分层提炼法，适配“渐进式常青化”
- 论证挖掘与话语结构：Argument Mining、Rhetorical Structure Theory（RST）
- 概念抽取与话题分割：TextRank、YAKE、TextTiling、KeyBERT
- 工具与实践：Obsidian/Logseq 双向链接；Linking Your Thinking（Nick Milo）；Heptabase 工作流
- 评估与图谱：知识图质量指标综述、开放本体设计入门（上下位与类型划分方法）

如果你提供一篇目标长文或其领域偏好，我可以按上述模板直接给出首批候选“常青笔记”与链接图建议。

原始想法：做一个帮我把长文拆成“常青笔记”的知识加速器