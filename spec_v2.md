# Idea2File Spec：V2

**Idea2File** 是一款轻量级命令行（CLI）工具，
帮助 AI 产品经理捕捉初始灵感，并即时将其扩展为结构化的 Markdown 笔记。
本版本（V2）的目标是提升**笔记的组织性与检索效率**，通过“自动分类”与“索引生成”功能实现智能归档。

---

## 一、核心功能（已有功能）

* 用户可以通过命令行输入一个想法（idea）。
* 系统基于该想法进行内容扩展，生成完整的结构化 Markdown 笔记。
* 笔记将自动保存为新的 `.md` 文件。

---

## 二、新增功能（V2）

### 功能1：自动分类（Automatic Categorization）

**功能目标**：笔记生成后，系统需自动判定其分类，并将文件移动至对应子文件夹。

**功能说明**：

- 系统需分析生成的 Markdown 文件完整内容。
- 预定义分类包括：`[通用心智]`、`[设计模式]`、`[产品方法论]`、`[战略洞察]`。
- 若能高置信度判定分类，将 `.md` 文件移入对应文件夹（示例：`notes/技术笔记/your-note.md`）。
- 若无匹配分类，放入默认的 `[其他]` 文件夹。

**实现细节**：

* 新增独立函数 `categorize_and_move_file(filepath)`。

  * 输入参数：生成的 Markdown 文件路径（绝对路径）。
  * 输出结果：文件移动后的新路径（字符串）。
* 分类逻辑与文件移动操作应在生成笔记后立即执行。

---

### 功能2:索引文件生成（Index Generation）


**功能目标：**
系统应自动更新索引文件 `index.md`，列出所有已生成的笔记，方便用户全局浏览。

**功能说明：**

* 每条索引项需包含：
  * 笔记标题（取 Markdown 文件第一行标题或文件名）
  * 生成日期（文件创建日期）
* 每条索引以以下格式记录：

  ```
  - [标题](./相对路径/文件名.md) — YYYY-MM-DD
  ```
* 按时间倒序排列（最新文件在最上方）。
* 若 `index.md` 不存在，应由系统自动创建。

**实现细节：**

* 新增函数 `update_index()`。

  * 在每次生成并分类新笔记后自动调用。
  * 函数需读取目录下的全部笔记文件并更新索引。

---

## 三、实现说明（Implementation Notes）

* 整个应用保持为**单一 Python 脚本文件**（`idea2file.py`）。
* 仅使用 Python 标准库，不依赖任何第三方包。
* 程序需可在 macOS 终端中直接运行：

  ```
  python3 idea2file.py
  ```
* 文件编码统一为 `UTF-8`。
* 系统需在控制台输出关键执行日志（生成、分类、索引更新）。

---

## 四、技术约束（Technical Constraints）

* **编程语言**：Python 3.x
* **CLI交互**：使用内置 `input()`，不使用外部 CLI 库。
* **文件操作**：仅使用 `os`、`shutil`、`datetime` 等标准库。
* **设计原则**：

  * 保持代码结构简洁明晰；
  * 优先保证可维护性与清晰性，而非复杂功能；
  * 所有函数均需有明确输入与输出；
  * 每次运行应保持独立、无状态。